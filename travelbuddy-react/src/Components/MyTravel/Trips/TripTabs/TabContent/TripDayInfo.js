import React from 'react';

const TripDayInfo = ({ dayNumber }) => {
    return (
        <div className="tab-content">
            <p>Content for Day {dayNumber} will go here.</p>
            {/* Add more content or components for day-specific information */}
        </div>
    );
};

export default TripDayInfo;